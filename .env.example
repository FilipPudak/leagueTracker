# Copy this file to .env and fill in your real values.
# .env is git-ignored and must never be committed.

# Google Sheets ID to use as the league database
SPREADSHEET_ID=your-spreadsheet-id-here

# Published URL of the backend Apps Script web app (/exec) that the static
# client (docs/app/app.js) calls directly. Bake this value into app.js API_URL.
API_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# SWU league website scraped for player sync (optional)
SCRAPE_URL=https://stockholm.sw-unlimited.com/
