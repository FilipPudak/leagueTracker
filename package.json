{
  "name": "swu-league-voting",
  "version": "1.0.0",
  "private": true,
  "description": "Google Apps Script voting/leaderboard app (backend + frontend). Tests use an in-memory GAS mock.",
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  }
}
