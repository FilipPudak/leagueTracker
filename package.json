{
  "name": "swu-league-voting",
  "version": "1.0.0",
  "private": true,
  "description": "SWU League Voting app — GAS backup + Cloudflare Worker backend with D1 database.",
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
