{
  "name": "league-tracker-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker backend for SWU League Voting",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "node src/db/migrate.js",
    "test": "node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
